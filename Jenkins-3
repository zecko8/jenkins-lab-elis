pipeline {
    agent { dockerfile true }

    stages {
        stage('Hello') {
            steps {
                sh 'node --version'
                sh 'git --version'
                sh 'curl --version'
            }
        }
    }
}
